export enum Constantes {
  maxTemperatura = 400,
  temperaturaAlerta = 330,
  capacidadMaxReactor = 700,
  minTemperatuta = 280,
  // Las tablas van a tener que ser modificadas a ser variables en un objeto
}
